# docs/ — screenshots & media

Image assets referenced by the top-level `README.md`:

| File         | What it shows                                                    |
|--------------|------------------------------------------------------------------|
| `paint.png`  | The `paint` app maximized — title bar, palette, tools, taskbar   |
| `shell.png`  | The interactive shell prompt + bottom status bar                 |
| `apt.png`    | `apt update` fetching the Debian index over TLS 1.3              |

PNG is preferred for crisp text. A short GIF of `paint` in action is also welcome
(reference it from the README if you add one).
